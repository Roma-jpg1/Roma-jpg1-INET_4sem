apiVersion: apps/v1
kind: Deployment
metadata:
  name: post-service
  namespace: likes-s18
spec:
  replicas: 2
  selector:
    matchLabels:
      app: post-service
  template:
    metadata:
      labels:
        app: post-service
    spec:
      containers:
        - name: post-service
          image: ghcr.io/example/likes-s18-post-service:latest
          ports:
            - containerPort: 8000
          readinessProbe:
            httpGet:
              path: /health
              port: 8000
            initialDelaySeconds: 8
            periodSeconds: 5
          livenessProbe:
            httpGet:
              path: /health
              port: 8000
            initialDelaySeconds: 20
            periodSeconds: 10
          resources:
            requests:
              cpu: 100m
              memory: 128Mi
            limits:
              cpu: 300m
              memory: 256Mi
---
apiVersion: v1
kind: Service
metadata:
  name: post-service
  namespace: likes-s18
spec:
  selector:
    app: post-service
  ports:
    - name: http
      port: 8000
      targetPort: 8000
  type: ClusterIP
