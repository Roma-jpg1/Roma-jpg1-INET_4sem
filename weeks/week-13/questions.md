# Вопросы для самопроверки

1. Зачем нужен Helm? Какую проблему он решает?
   - Helm позволяет шаблонизировать Kubernetes-манифесты и переиспользовать их для разных окружений.
2. Как работает приоритет значений (Values)? Кто победит: `values.yaml` или флаг `--set`?
   - `--set` имеет более высокий приоритет, оно перекрывает значения из `values.yaml`.
3. Что такое функция `include` и `_helpers.tpl`? Зачем они нужны?
   - `include` вставляет повторяемый фрагмент из `_helpers.tpl`, чтобы не дублировать шаблоны.
4. Почему в шаблонах Helm так важно следить за отступами и зачем нужна функция `indent` или `nindent`?
   - Потому что в YAML отступы определяют структуру, а `indent`/`nindent` сохраняют правильный формат.
5. Как откатить неудачный релиз с помощью Helm?
   - `helm rollback <release> <revision>`.
6. Можно ли использовать Helm только для генерации YAML, без установки в кластер?
   - Да, команда `helm template` рендерит YAML без деплоя в кластер.
